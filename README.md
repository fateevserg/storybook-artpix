# storybook-artpix

This project contains a minimal Storybook setup. To run Storybook locally:

```bash
npm install
npm run storybook
```

To build a static version of the Storybook:

```bash
npm run build-storybook
```
